# Classes.py
conssits of the most important classes, inlcuding what i learned along the way

currently only @dataclass, still working on that!

# Convert_sum.py
simply adds two numbers together with primitive error handling

i MAY change this in the future to be a bit more robust and efficient
